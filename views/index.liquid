<!DOCTYPE html>
<html>
  <head>
      <!-- CSS
      –––––––––––––––––––––––––––––––––––––––––––––––––– -->
            
      <!-- <link rel="stylesheet" href="../static/css/custom.css"> -->

      <!-- FONT
      –––––––––––––––––––––––––––––––––––––––––––––––––– -->
      
      <!-- <link href='//fonts.googleapis.com/css?family=Raleway:400,300,600' rel='stylesheet' type='text/css'> -->

  </head>
  <body>
    <div id="app"></div>
    <hr>
    <div>
      <p>Are we authenticated?: {{ authenticated }}</p>
    </div>
    <hr>

    {% comment %}
    Give the option to login.
    {% endcomment %}

    {% if authenticated == false  %}
      <div>
        <a href="/login">Login</a>
      </div>
      <hr>
    {% endif %}
    
    <div>
      {{ bookmarks }}
    </div>

    <!-- SCRIPTS  -->

    {% comment %}
    Do not serve the sync button unless we are authenticated.
    {% endcomment %}

    {% if authenticated %}
      <script src="app-bundle.js">
      </script>
    {% endif %}

  </body>
</html>
